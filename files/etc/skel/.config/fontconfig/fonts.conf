<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<!-- Delete /etc/fonts/conf.d/[2-46-9]*.conf so that our settings are not overridden. -->
<fontconfig>
  <include ignore_missing="yes">conf.d</include>
</fontconfig>
